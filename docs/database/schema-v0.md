# Schema v0 (Phase 1 — no product tables yet)
Baseline extensions only (see infrastructure/postgres/init.sql).
Phase 2 adds: users, sessions, refresh_tokens, email_verification_tokens, password_reset_tokens.
