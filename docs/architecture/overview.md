# Architecture (Phase 1 stub)
Monorepo: apps/{desktop,web,server}, packages/{shared,models,validation,api-client,ui,config}.
Backend: Express (JS). Realtime: Socket.IO (Phase 6). DB: PG, Redis, MinIO.
